
botnet = require '../botnet'

{type} = Function

# cluster events, emitted to workers

module.exports = {
	message: (message) ->
		return false if type( message ) isnt 'object'

		# message is { cmd: '', args: [] }

		if message.cmd is 'emit'
			botnet.emit.apply botnet, message.args
}