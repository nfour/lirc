{
	"name": "lirc",
	"version": "0.6.0",
	"description": "An old IRC client for node",
	"scripts": {
		"build": "./scripts/build.bash",
		"release": "cd build && npm publish",
		"preversion": "yarn build",
		"test": "jest -i src"
	},
	"dependencies": {
		"coffee-script": "1",
		"lactate": "0.13.x",
		"lance": "^2.10.14",
		"socket.io": "0.9.x",
		"ssl-keygen": "0.1.x"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/nfour/lirc"
	},
	"devDependencies": {
		"jest": "^21.1.0",
		"jest-coffee-preprocessor": "^1.0.0"
	}
}
